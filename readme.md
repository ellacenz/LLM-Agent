# SQL LLM Agent

This LLM-powered agent converts user inputs, instructions, or questions into precise SQL queries to retrieve relevant information from a database. It functions as an expert SQL assistant, capable of generating, explaining, and executing SQL queries to provide accurate and context-aware answers.

- pip install python-dotenv
- pip install -U agno openai
- pip install mysql-connector-python
